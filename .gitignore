node_modules/
.DS_Store
.env
.env.*
!.env.example
.unicorn-hub/local.json
.claude/
.codex/
.omc/
.omx/
worktrees/
__pycache__/
*.py[cod]
.pytest_cache/
dist/
coverage/
# Encrypted runtime state written by the bot (feature 005); never commit it.
ha_state.enc
ha_state.enc.tmp
*.enc
*.enc.tmp
# Bind-mounted writable state for the production container.
runtime-data/
# Host-only deployment lock and stable-image ledger (never mounted into the bot).
.deploy-state/
